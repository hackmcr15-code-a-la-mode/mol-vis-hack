# !/usr/bin/env python

import sys
import os

for arg in sys.argv:
    if (arg.startswith('basedir:')):
        basedir = arg.split('basedir:')[1]
        vrml_filename = 

        # This is the base directory, used for saving files
#        molecule = bpy.ops.import_scene.x3d(
#        filepath = vrml_filename,
#        )
        basedir+'/structures/wrl'+'/'+pdb_code+'.wrl')
